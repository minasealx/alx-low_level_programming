#include <stdio.h>
#include "main.h"
/**
 * _isupper - Is the enter
 * @c: the integer to be checked
 * Return: the value
 */

int _isupper(int c)
{
	if (c >= 65 && c < 91)
{
	return (1);
}
	else
{
	return (0);
}
}
